(ns myrf.db)

;; (def default-db {:active-page :home})

(def default-db {:active-page :home
                 :theme "skeleton"
                 :loading false
                 :products {1 {:title "test"
                               :price 23}}})